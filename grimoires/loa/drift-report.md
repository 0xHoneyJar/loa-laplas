# Three-Way Drift Analysis

> Code reality vs documentation vs manifest. Generated by /ride 2026-06-12.
> Code is truth; every status below cites the evidence checked.

## Summary

| Category | Count | Items |
|----------|-------|-------|
| Aligned | 14 | Core architecture claims (pipeline, seams, schemas, hooks, gates) |
| Stale | 5 | D1 test counts, D2 manifest contributes, D3 manifest tests, D5 README model-routing, D7 README component table |
| Missing/Shadow | 3 | D6 legba absent from README, D8 second test root, D9 version lag |
| Hallucinated | 0 | — |
| Ghost | 0 | — |

**Drift score: 8.2/10** (healthy — drift is concentrated in enumerative/inventory
sections that lag a fast-moving codebase; zero fabricated claims; the architectural
descriptions are accurate to the line).

## Stale (docs exist, code changed) — HIGH impact

### D1 — README test counts (README.md:72, 157-168)
- Claims: "115 @test across 8 suites"; per-suite table (form-c-dispatch 34, etc.)
- Reality: **223 @test across 11 suites** in tests/integration/ (form-c-dispatch.bats: 102,
  compose-verify-run.bats: 25, compose-terminal-gate.bats: 10, adapter-gen-env-override.bats: 5
  all unlisted) **+ 13 @test in tests/composition/state/** (2 suites) + 8 legba node-test asserts.
- Evidence: `grep -c '@test' tests/integration/*.bats` (see reality/test-files.txt)

### D2 — construct.yaml `contributes` (construct.yaml:71-94)
- Lists 10 scripts + 2 lib + 1 template + 3 data + 2 hooks + 1 doc.
- Missing from the list: compose-verify-run.sh, compose-doctor.sh, compose-handoff-wrap.sh,
  compose-seam-clew.sh, compose-output-schema-preflight.sh, surface-envelope.sh,
  pair-relay-validate.sh, scripts/clew/ (8 files), scripts/legba/ (7 files),
  lib/compose-cut.py, lib/segment-emitter.py, lib/run-emitted-segment.js,
  lib/workflow-syntax-check.js, lib/compose-cost-card.py,
  data/trajectory-schemas/pair-relay-composition.schema.json.
- Impact: install-time sync (which copies per `contributes`) would omit the entire
  Form C compiler core if the manifest is the sync source of truth. **Highest-impact drift.**

### D3 — construct.yaml `tests.suites` (construct.yaml:97-104)
- Lists 4 suites; 13 exist (11 integration + 2 composition/state).

### D4 — construct.yaml `requirements` (construct.yaml:107-122)
- Declares claude-code, python3 (jsonschema/pyyaml/rfc8785), bash, yq, jq.
- Missing: **node** (run-emitted-segment.js, workflow-syntax-check.js, legba/*.mjs are
  load-bearing) and **bats** (the declared test runner).

### D5 — README "Model routing & token cost (open design)" (README.md:202-206)
- Claims: substrate does not decide models; `model: inherit`; overrides manual;
  `LOA_ROOMS_DEFAULT_MODEL` not implemented.
- Reality: segment-emitter.py:130-182 now implements tier routing
  (tiny→haiku, cheap→sonnet, mid/standard/else→opus; honors capabilities.model_tier /
  downgrade_allowed; relative gate floor) — commits 928c356, 05d54fb, c3d4446 (#40, #41).
- Note: routing commits are on branch feat/issue-40-cognitive-load-routing, not yet on
  main — on main this section is only *partially* stale (the open-design framing predates
  even the SoT-read posture described in the operator kernel). Fold the README update into
  the #40 PR (see hygiene H6).

### D7 — README component table (README.md:55-72)
- Missing rows for shipped components: compose-verify-run.sh (proof-of-run terminal gate),
  compose-doctor.sh, compose-output-schema-preflight.sh, compose-cost-card.py,
  scripts/legba/ (custody-chain verification CLI), /compose skill (skills/compose/SKILL.md).

## Missing / Shadow (code exists, undocumented at repo front door) — MEDIUM impact

### D6 — Legba subsystem (scripts/legba/, 1,089 lines + 248 test lines)
- Whole cryptographic-verification subsystem (spans/gates/ed25519 custody/receipt hash,
  verify + challenge verbs) absent from README.md and construct.yaml.
- Has its own good README (scripts/legba/README.md) — shadow only at the front door.

### D8 — Second test root (tests/composition/state/)
- recent-learnings.bats (8) + clew-hook-never-blocks.bats (5) not reachable from the
  documented `bats tests/integration/` invocation (README.md:193).

### D9 — Version stamp lag
- v0.3.0 (tag + manifest + README badge) predates legba (#37-39) and the terminal-gate
  work on main. See hygiene H3.

## Hallucinated / Ghost — NONE FOUND

Every checked README capability claim resolves to real code (claims-to-verify.md A1-A9,
D1-D6, T1-T5 — 14 VERIFIED). Notably honest docs: the boundaries section (NOT a security
sandbox — confirmed log-only at loa-tool-mandate.sh:140), legba's PROVISIONAL banner, and
Form A/B "not gone yet" (both modes still live in compose-dispatch.sh).

## Critical items

1. **D2** — fix `contributes` before any consumer relies on manifest-driven sync.
2. **D1 + D7** — README inventory refresh (test counts + component table + legba row).
3. **D4** — declare node + bats requirements.
4. **D5** — update model-routing section inside the #40 PR.
