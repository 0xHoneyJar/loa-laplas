# Code Hygiene Report

> Generated by /ride 2026-06-12. Items flagged for HUMAN DECISION — nothing fixed.

## Summary

Overall hygiene is high: consistent naming, single-purpose scripts, fail-closed
validators, near-zero tech debt markers (2 real TODOs in ~9,000 lines of script source).

## Flagged Items

### H1 — Untracked `grimoires/` and `.claude/` (intentional? confirm)
- `git status` shows `?? grimoires/`, `?? .claude/`, `?? scripts/lib/__pycache__/`
- `.claude/scripts`, `.claude/hooks`, etc. ARE gitignored (mount-submodule symlinks, .gitignore:4-14),
  but the `.claude/` directory itself and `grimoires/` are untracked-without-ignore.
- README.md:228 says grimoire artifacts are deliberately local-only. If so, add
  `grimoires/` to .gitignore to make the intent mechanical instead of accidental.
- **Decision needed**: ignore vs track.

### H2 — `scripts/lib/__pycache__/` pollution
- Python bytecode cache untracked in working tree. Add `__pycache__/` to .gitignore.
- **Decision needed**: trivial .gitignore addition (micro-fix class, but repo-config — flagged not fixed).

### H3 — Version stamp lag
- `construct.yaml:4` and README badge say v0.3.0; highest git tag is v0.3.0, yet
  main has since absorbed legba (#37, #38, #39) and the compose terminal gate /
  verify-run suites. Either the post-merge simple-release tagging didn't fire for
  these merges or the manifest version isn't being bumped.
- **Decision needed**: reconcile version stamps with release pipeline.

### H4 — Vendored copy divergence risk (`scripts/clew/`)
- `scripts/clew/VENDORED.md:49` records a local fix to `ledger-append.sh` (slug
  normalization `slug="${slug#construct-}"`) that is NOT yet applied upstream
  (loa-constructs#249). Silent-loss class bug if upstream re-vendors without it.
- **Decision needed**: upstream the 1-line fix before the next vendor sync.

### H5 — `tests/composition/` naming asymmetry
- Test suites live in both `tests/integration/` (11 suites) and
  `tests/composition/state/` (2 suites). `bats tests/integration/` (README.md:193)
  does NOT run the composition/state suites. CI entry point unclear.
- **Decision needed**: unify test invocation or document the second root.

### H6 — In-flight branch divergence
- Working branch `feat/issue-40-cognitive-load-routing` carries 3 emitter-routing
  commits (928c356, 05d54fb, c3d4446) not on main. README's model-routing section
  describes pre-#40 behavior even relative to this branch's own code.
- **Decision needed**: fold README update into the #40 PR.

## Not Flagged (checked, clean)

- No files outside standard directories (everything under scripts/, hooks/, data/,
  docs/, tests/, compositions/, templates/, skills/).
- No temporary/WIP folders in the app zone.
- No significant commented-out code blocks found in scripts.
- No dependency conflicts (zero package manifests by design — bash/python-stdlib/node-stdlib only;
  jsonschema/pyyaml/rfc8785 declared as system requirements in construct.yaml:111-113).
